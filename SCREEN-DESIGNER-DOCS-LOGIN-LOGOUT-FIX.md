# Screen Designer + Documentation + Authentication UI Fix

- Desktop Screen Designer component palette now has a dedicated full-height vertical scroll area.
- Palette search remains pinned while the component list scrolls.
- Documentation is an interactive page with filters, artifact summaries, preview, print and HTML export.
- Global Sign out button is always visible while authenticated, including mobile.
- Login remains the entry screen after sign out.
- No database migration is required.
